# Session du 05 juillet 2026 — Logycab

## Sujet : correction du champ DateRecrt (date de recrutement patient)

### Diagnostic
- Le champ existait et fonctionnait depuis 2004 dans l'ancien Access, mais n'a pas été
  repris lors de la migration vers SQL Server : 8411 patients sur 8423 avaient
  `DateRecrt = NULL`.
- Le code actuel (`ajax_patient.php`) fonctionne correctement pour les nouveaux patients —
  confirmé, aucun bug PHP à corriger.

### Action réalisée (base de données uniquement, aucun fichier PHP modifié)
1. Export des dates réelles depuis Access (requête SQL Access + export CSV)
2. Import dans SQL Server via `BULK INSERT` dans une table temporaire `Import_DateRecrt`
3. `UPDATE` de la table `ID` avec `TRY_CONVERT` pour récupérer les dates historiques
4. 8410 patients corrigés avec succès

### Résultat final
- 8409 patients : date récupérée depuis Access
- 12 patients récents : déjà corrects (créés via l'appli actuelle)
- 2 patients restent sans date (non récupérable) :
  - N°1888 (EL HADRI AMINA) — date invalide dans Access (00/01/1900)
  - N°4525 (EL MAKNOUZI MARIAM) — jamais eu de date, même dans l'ancien système

### Reste à faire
- Décider si on renseigne manuellement une date pour ces 2 patients
  (ex. date du premier acte dans ORD/LE_BILAN), ou si on les laisse vides
- Supprimer la table de brouillon : `DROP TABLE Import_DateRecrt;`

### Tâches en attente (inchangées)
- Tâche 12 (Lettre de correspondance) : test impression toujours reporté
- Tâche 13 (Révision listes bilan clinique) : statut à confirmer
